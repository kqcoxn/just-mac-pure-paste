# 第一版验证记录

验证日期：2026-09-16。

## 环境与产物

- 本机：Apple Silicon，macOS 27.0（26A428）。
- 编译器：Apple Swift 6.4；项目明确启用 Swift 6 语言模式。
- 构建：Command Line Tools，编译时指定本机 macOS 26.5 SDK，使用脚本中的兼容处理。
- 最低系统版本：macOS 14.0（应用 Info.plist 与 Mach-O 最低部署版本均为 14.0）。macOS 14 本机实测未完成。
- 产物：`dist/Just Pure Paste.app`，arm64，版本 0.1.0，非沙盒，ad-hoc 签名。
- KeyboardShortcuts 固定为 3.1.0，提交 `772133d9dbe800fdac0473226822994c5c162c58`。兼容转换与资源路径调整见 README。

## 已通过

| 检查 | 结果 |
| --- | --- |
| 自动测试 | `./scripts/swift.sh test`：13 项测试，2 个测试套件，含参数化用例，全部通过 |
| Release 构建与打包 | `./scripts/build-app.sh` 成功生成 `.app` |
| 签名 | `codesign --verify --strict` 通过 |
| 依赖资源 | 本地化 bundle 与 MIT 许可证随应用放入 `Contents/Resources`；中文录制控件正常显示 |
| 首次启动 | 自动展示设置，显示默认 ⇧⌘V、权限状态和说明 |
| 设置布局 | 通过实际截图检查，文字和按钮无裁切 |
| 快捷键自定义 | 实际录制为 ⌥⇧⌘P，退出、重启后设置中仍显示该组合 |
| 快捷键清除 | 清除后退出、重启仍为空，未被初始默认值覆盖 |
| 恢复默认 | 点击“恢复默认”后显示 ⇧⌘V；交付时保留此默认配置 |
| 普通粘贴冲突保护 | 尝试录制 ⌘V 时，控件提示与“粘贴”菜单项冲突，未覆盖现有快捷键 |
| 再次启动与重新打开 | 后续冷启动仅驻留；对运行中的 `.app` 再次执行打开操作可显示设置 |
| 退出 | 设置界面的 ⌘Q 可退出应用 |

自动测试包含：

- 文本 Unicode、空格、制表符与换行保真，去除 HTML/富文本类型。
- 仅 RTF 内容的文本提取，以及混合图片和文本的文本读取。
- 空剪贴板、空字符串、图片和文件复制均不修改剪贴板。
- 权限缺失、无目标应用、按键创建失败、写入失败时不发送粘贴。
- 等待修饰键释放、超时、等待期间的目标切换、权限撤销和剪贴板更新。
- 读取后再次验证环境；重复触发不并发；取消后可接受下一次操作。

剪贴板测试使用 `NSPasteboard.withUniqueName()`；模拟按键使用测试替身，不在测试套件中向真实应用发送按键。

## 待授权验证

用户选择“暂不授权，先交付应用”，因此没有授予辅助功能权限，也没有执行真实跨应用粘贴。

| 应用/场景 | 状态 |
| --- | --- |
| Safari 输入框 | 待授权验证 |
| VS Code 临时文本 | 待授权验证 |
| 微信草稿输入框 | 待授权验证，不发送消息 |
| WPS 临时文档 | 待授权验证 |
| 系统剪贴板访问提示与授权后的读取 | 待验证 |
| 实际目标应用接收 CGEvent 的兼容性 | 待验证 |
| 从菜单栏点击设置、关闭设置后继续触发 | 待完整手工验证；设置重开与运行生命周期已有部分验证 |

本次未实际验证完整 Xcode 构建、Intel Mac、macOS 14 或其他旧系统，也未进行公证与跨机器分发测试。

用户要求停止界面控制后，未继续操作应用窗口；剩余界面验证留待后续。

## 更新检查功能验证

2026-09-16 新增更新检查后：

- `./scripts/swift.sh test`：28 项测试、6 个套件全部通过，包含原有粘贴测试。
- 新增测试覆盖语义版本数字排序、预发布与正式版比较、非法版本、Release 响应解析、固定仓库下载地址、无令牌请求、HTTP 404/403/429/500、失败保留已发现版本、设置持久化、24 小时间隔、手动一分钟限流、重启后节流、重复检查合并及取消结果丢弃。
- 使用公开、无认证请求检查本仓库 `releases/latest`，当时返回 HTTP 404；这对应“暂未找到可用的公开正式版”，不将其误报为已是最新。
- Release 构建、ZIP 打包、解压签名校验通过；正式产物 `JPPReleaseTag=v0.1.0`。
- 实际验证预发布构建可写入 `JPPReleaseTag=v0.1.0-beta.1`，且标签与数字版本不匹配会在构建前失败；交付产物已重新构建为默认正式版本。
- GitHub Actions 语法和 shell 语法检查通过，CI 将完整标签写入应用，避免预发布版本丢失后缀。
- 按用户要求未进行界面控制或启动应用。新设置区、菜单栏提示及打开下载页仍需用户手工验证；后台定时行为通过注入时间的自动测试验证，未进行真实 24 小时等待测试。
